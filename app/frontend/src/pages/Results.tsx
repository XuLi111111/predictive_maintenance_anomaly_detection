import Footer from "../components/Footer";
import Nav from "../components/Nav";

export default function Results() {
  return (
    <>
      <Nav active="results" />
      <section className="stub">
        Results page — to be implemented by teammate.
        <br />
        Required: probability chart with shaded alert bands, summary panel,
        8-model comparison grid, PDF export (FR-14–20).
      </section>
      <Footer />
    </>
  );
}
